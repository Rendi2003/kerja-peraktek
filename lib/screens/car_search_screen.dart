import 'package:flutter/material.dart';

class CarSearchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Search Car')),
      body: Center(child: Text('Car Search Page')),
    );
  }
}
