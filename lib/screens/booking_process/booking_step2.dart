import 'package:flutter/material.dart';

class BookingStep2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Booking Step 2')),
      body: Center(child: Text('Booking Step 2')),
    );
  }
}
