import 'package:flutter/material.dart';

class BookingStep1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Booking Step 1')),
      body: Center(child: Text('Booking Step 1')),
    );
  }
}
