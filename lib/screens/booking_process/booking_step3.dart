import 'package:flutter/material.dart';

class BookingStep3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Booking Step 3')),
      body: Center(child: Text('Booking Status')),
    );
  }
}
